'use babel';

import DynamicThemeSwitcherView from '../lib/dynamic-theme-switcher-view';

describe('DynamicThemeSwitcherView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
